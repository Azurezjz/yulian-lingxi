# 后端依赖包
# Python 版本要求：>= 3.9

# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0

# HTTP 客户端
httpx==0.25.2
requests==2.31.0

# 大模型相关（根据实际使用的模型选择）
openai>=1.0.0  # OpenAI API 客户端（支持 OpenAI 和兼容 API）
# transformers==4.35.0  # 如果使用本地模型（可选）
# langchain==0.0.350  # 工具链框架（可选）
# langchain-community==0.0.10  # LangChain 社区工具（可选）

# 工具 API 相关
requests>=2.31.0  # HTTP 请求库（用于调用天气、新闻、股票 API）

# 数据处理
pandas==2.1.3
numpy==1.26.2

# 文档生成
python-docx==1.1.0  # Word 文档生成（可选）
markdown==3.5.1

# 数学计算
sympy==1.12  # 符号计算（可选）

# 工具类
python-dotenv==1.0.0  # 环境变量管理
pydantic==2.5.0  # 数据验证
pydantic-settings==2.1.0  # 配置管理

# 日志
loguru==0.7.2  # 日志库（可选，或使用标准库 logging）

# 测试
pytest==7.4.3
pytest-asyncio==0.21.1

